# Cross-platform runtime dependencies. PyTorch installs the appropriate
# CPU build by default; CUDA is used automatically when a compatible GPU build
# is installed separately.

flask>=3.0.0
# Flask's own app.run() looks for python-dotenv to load .env files; without
# it, Flask tries to print a tip via click/colorama, which can crash on some
# Windows consoles (OSError: Windows error: 6). Installing it avoids that
# code path entirely (this project's own config loader does not depend on it).
python-dotenv>=1.0.0
opencv-python>=4.8.0
mediapipe>=0.10.0
ultralytics>=8.0.0
torch>=2.2.0
torchvision>=0.17.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
pyyaml>=6.0.0
pillow>=10.0.0
ipywidgets>=8.0.0
reportlab>=4.2.0
requests>=2.31.0
# Pretrained, CPU-compatible OCR for exact Iron and Wedge markings.
rapidocr>=3.9.0,<4.0.0
onnxruntime>=1.20.0

# Test tooling
pytest>=7.4.0
